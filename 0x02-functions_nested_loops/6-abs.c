#include "holberton.h"
#include <stdlib.h>
/**
 * _abs - return absolute number
 * @num: number to check
 *
 * Return: the absolute number
 */
int _abs(int num)
{
	int num_abs;

	num_abs = abs(num);
	return (num_abs);
}
